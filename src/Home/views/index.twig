{% extends 'layout.twig' %}

{% block body %}

    <h1>{{ page.title }}</h1>

    <!-- TODO Des témoignage pour me présenter ? trouver un truc  -->
    <p>
        {{ page.content }}
    </p>

    <h1>Générateur de citations</h1>

    <blockquote id="display-quote">
        <p>{{ item.proverb }}</p>
        <br>
        <cite>- De {{ item.proverbAuthor }}.</cite>
    </blockquote>
    <p>
        <a class="btn btn-primary" id="new-quote" href="/">Citations</a>
    </p>


{% endblock %}