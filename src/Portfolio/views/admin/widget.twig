<div class="col-sm-3">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">{{ count }}</h4>
            <p class="card-text">Réalisation{% if count > 1 %}s{% endif %}</p>
            <a class="btn btn-primary" href="{{ path('portfolio.admin.index') }}">Gérer</a>
        </div>
    </div>
</div>